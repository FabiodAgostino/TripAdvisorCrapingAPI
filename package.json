{
  "name": "salento-scraping-api",
  "version": "1.0.0",
  "type": "commonjs",
  "description": "API Serverless per scraping ristoranti TripAdvisor - Salento",
  "main": "api/scrape.ts",
  "scripts": {
    "start": "echo 'Usa vercel dev per sviluppo locale'",
    "build": "tsc --noEmit",
    "test": "echo \"No tests yet\" && exit 0",
    "deploy": "vercel --prod",
    "deploy-preview": "vercel"
  },
  "dependencies": {
    "@vercel/node": "^3.0.14",
    "axios": "^1.6.2",
    "cheerio": "^1.0.0-rc.12"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}